[pytest]
addopts = -q --cov=services --cov=core --cov=api --cov=utils --cov-branch --cov-report=term-missing --cov-fail-under=85
asyncio_mode = auto
markers =
    load: marks tests as load/soak tests (deselect with '-m "not load"')

